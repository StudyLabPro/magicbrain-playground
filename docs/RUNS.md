# Фактические прогоны

Все числа получены в поднятом стеке через тот же
HTTP-интерфейс, которым пользуется человек. Документ **генерируется** скриптом
`scripts/render_runs.py` из файлов `var/runs/*.json` — переписать число в нём
и не переписать в прогоне нельзя.

Версия кода: MagicBrain `8cf97fa9b8db`
(рабочее дерево на момент сборки — `clean`; в образ попал
`git archive HEAD`, чужие незакоммиченные правки исключены),
плейграунд `ad9b29f9a865`,
balansis 1.1.0, numpy 1.26.4, сборка `20260908-194643-8cf97fa9b8db`.

> Оговорка о времени: хост в момент прогонов был занят параллельной работой
> других агентов (load average 10–16 на 8 ядрах). Абсолютные секунды поэтому
> завышены и бенчмарком скорости считаться не могут;
> сравнения внутри одного прогона (ACT против numpy, стадия против стадии)
> остаются осмысленными, потому что стороны считались последовательно в
> одинаковых условиях.


## Сценарии

### а) Обучение на публичном корпусе

Прогон `train-20260908-194756-7279f7` · данные: `shakespeare_en` (D0), 100000 символов, sha256 `b0894691ca70` · сеть: N=1024, K=12,
рёбер 13025, геном `30121033102301230112332100123` · 100000 шагов ·
104.5 с (1015 шагов/с).

| модель | бит/символ на отложенном хвосте |
|---|---|
| равномерная | 5.931 |
| униграмма | 4.794 |
| биграмма | 3.437 |
| сеть до обучения | 5.996 |
| **сеть после обучения** | **4.666** |

Бьёт униграмму: **да**.
Бьёт биграмму: **нет**.

Что сеть генерирует (первые 200 символов, затравка — начало текста):

```
First Citizen:
Before we proceed any furrsrtr hdroos t rah rsh  ooh uo shd hahe tr  e onoa or lo srrrsi  uneaharu se st tnr e a  t iestst, tnn a   u tt ,tihie hathhhenthu r r lrehod a ws  ,uaer wm r
n
```

### а') Обучение на синтетике с известной энтропией источника

Прогон `train-20260908-194941-0c5936` · данные: `synthetic_markov` (D2), 80000 символов, sha256 `787ffb2b110e` · сеть: N=1024, K=12,
рёбер 13025, геном `30121033102301230112332100123` · 100000 шагов ·
80.1 с (1327 шагов/с).

| модель | бит/символ на отложенном хвосте |
|---|---|
| равномерная | 4.459 |
| униграмма | 4.115 |
| биграмма | 1.638 |
| сеть до обучения | 4.570 |
| **сеть после обучения** | **2.998** |

Бьёт униграмму: **да**.
Бьёт биграмму: **нет**.

Скорость энтропии генератора — 1.152 бит/символ. Это оценка самого источника, а не результат модели: строка разбирается на шаги однозначно, поэтому энтропия одного шага, делённая на его среднюю длину, и есть предел, к которому может стремиться модель, знающая параметры цепи; обучающейся на выборке модели опуститься ниже неё на отложенном хвосте в среднем не даёт ничего. Аналитический расчёт сходится с эмпирической оценкой по фактически сделанным выборам: 0.9538 нат/шаг у цепи плюс 0.9408 у разделителей на 2.3733 символа шага против 0.7991 нат/символ накопленных при генерации. Сеть остановилась на 2.998.

Что сеть генерирует (первые 200 символов, затравка — начало текста):

```
баразяноми гидобатувежужусоразяжужу
батуеруми гидогиеоу геддобаао  бадоса раогмбаабиугии уборау а доус арво ба до рарраоатвддово оад доо
аорао м оммидодоаоа ародораидоаоодоуоододо ом до   боуаражбасиа
```

### б) Пайплайн нейрогенеза

Прогон `neurogenesis-20260908-195101-f64095` · данные: `rachinsky_ru` (D0), 60000 символов, sha256 `65424be83078` · стратегия
`statistical` · 100000 шагов · 107.2 с.

| стадия | секунд |
|---|---|
| Компиляция генома из статистики текста | 0.07 |
| Развитие ткани: позиции, связи, веса из CPPN, созревание порогов | 0.10 |
| Обучение развитой сети на том же тексте | 100.70 |
| Авторегрессивное воспроизведение текста из обученной сети | 0.25 |
| Верность воспроизведения, отношение размеров, аттракторы | 0.89 |

**Компиляция.** Геном `3332313212311030210210233220` длиной 28; оценка качества
генома 0.767. Статистика текста: энтропия
4.97 нат/символ, словарь
86, повторяемость
0.853.

**Морфогенез.** Нейронов 1216, рёбер 25779, CPPN
использован; веса ткани
mean=-0.0078, std=0.0192; тормозных
9.4%; средний порог после созревания
0.0089. Развитие заняло 0.06 с.

**Обучение.** Отложенный хвост 6.457 →
4.722 бит/символ при биграмме
3.640; биграмму
**не обошёл**.

**Воспроизведение и оценка.** Точность посимвольного совпадения
0.078, совпадение биграмм 0.302,
триграмм 0.053, пересечение словарей
0.444.

Отношение длины генома к размеру данных: **0.00028**
(28 байт генома на 100119 байт текста).
Для сравнения, доля от исходного размера у настоящих кодеков:
gzip 0.235, bz2 0.172, lzma 0.206 —
но кодек восстанавливает текст точно, а здесь совпадение символов
0.078. Это разные величины, и складывать их в одну
таблицу «во сколько раз сжали» нельзя.

Аттракторов найдено 1 на 100 проб;
размеры бассейнов [100]. Средняя доля активных
нейронов в найденном состоянии — 0.915.

Один аттрактор, в который сошлись все пробы, и почти все нейроны в нём активны — это не набор запомненных образов, а коллапс динамики в одно состояние «всё включено». Аттракторная память на этом прогоне не демонстрируется, и выдавать число «найден 1 аттрактор» за неё нельзя.

### в) Самовосстановление: повреждение рекуррентных синапсов

Прогон `self-repair-20260908-195249-183304` · цель повреждения
`recurrent` · та же обученная сеть
перечитывается с диска перед каждым повреждением · восстановление
8000 шагов · 94.2 с.

| доля | обнулено весов | до, бит/симв | сразу после удара | после восстановления | отыграно | остаточный ущерб |
|---|---|---|---|---|---|---|
| 10% | 1302 | 4.666 | 4.649 | 4.635 | не измеримо | -0.031 |
| 20% | 2605 | 4.666 | 4.644 | 4.636 | не измеримо | -0.030 |
| 40% | 5210 | 4.666 | 4.641 | 4.653 | не измеримо | -0.013 |

Все значения — на отложенном хвосте, который в дообучении не участвует.
«Отыграно» — доля просадки, которую вернуло дообучение; «не измеримо» значит,
что просадка меньше 0.01 нат/символ, то есть удар ничего заметного не сломал и
восстанавливать нечего.

Это штатный `TextBrain.damage_edges`. Результат неожиданный и важный: обнуление
до 40% рекуррентных синапсов почти не меняет качество предсказания. Значит,
рекуррентная часть в этой задаче несёт мало сигнала, а работу делает линейный
readout, которого такое повреждение не касается. «Восстанавливать» после такого
удара нечего, поэтому доля отыгранного здесь и не считается.

### в') Самовосстановление: повреждение линейного readout

Прогон `self-repair-20260908-195425-80adf6` · цель повреждения
`readout` · та же обученная сеть
перечитывается с диска перед каждым повреждением · восстановление
8000 шагов · 94.4 с.

| доля | обнулено весов | до, бит/симв | сразу после удара | после восстановления | отыграно | остаточный ущерб |
|---|---|---|---|---|---|---|
| 10% | 6246 | 4.666 | 4.691 | 4.660 | 125.2% | -0.006 |
| 20% | 12492 | 4.666 | 4.730 | 4.709 | 32.7% | 0.043 |
| 40% | 24985 | 4.666 | 4.844 | 4.763 | 45.8% | 0.096 |

Все значения — на отложенном хвосте, который в дообучении не участвует.
«Отыграно» — доля просадки, которую вернуло дообучение; «не измеримо» значит,
что просадка меньше 0.01 нат/символ, то есть удар ничего заметного не сломал и
восстанавливать нечего.

Здесь обнуляется доля весов матрицы readout `R` — той самой, что превращает
состояние сети в распределение по символам. Просадка видна сразу, и вот на ней
уже видно, восстанавливается сеть дообучением или нет.

### г) С ACT-бэкендом Balansis и без него

Прогон `act-20260908-195602-748a5e` · один геном, один seed
(20260908), один текст · по 10000 шагов на сторону ·
balansis 1.1.0.

| | numpy | ACT |
|---|---|---|
| ACT реально активен | False | True |
| отложенный хвост, бит/символ | 5.131 | 5.132 |
| время, с | 6.7 | 20.7 |
| шагов/с | 1489 | 483 |

ACT медленнее в **3.09×**; разница качества
0.00045 нат/символ — то есть на этой задаче
компенсированная арифметика ничего не меняет в предсказании.

Где она всё же меняет ответ — микробенчмарк на специально плохо обусловленных
данных, эталон `math.fsum`:

| операция | ошибка numpy | ошибка ACT | точнее в |
|---|---|---|---|
| сумма 30000 чисел | 5.726e-06 | 0.000e+00 | ∞× |
| скалярное произведение (10000) | 1.776e-14 | 0.000e+00 | ∞× |

Вывод без прикрас: ACT честно делает то, ради чего написан — в микробенчмарке
его ошибка ровно ноль против эталона. Но веса `TextBrain` — float32 в диапазоне
порядка 0.01–0.1, где наивная сумма и так точна. На этой задаче мы платим
замедлением в 3.09× за точность, которая не нужна.

### д) Сохранение и повторное открытие

Все 6 прогонов выше сохранены файлами в `var/runs/` и после завершения перечитаны через HTTP; сериализованный JSON сравнивался целиком, совпали 6 из 6. Модель прогона
`train-20260908-194756-7279f7` передана в соседний контейнер со штатным MagicBrain API
(`POST /api/v1/runtime/models/{id}/load`), после чего его собственный рантайм
сгенерировал текст:

```
The adehate i tith rorie ho  lenan t   ta tno let  ahtes e t e ehler  aoo i rtoh  athena   ll e ld ala

 a e ie i a
r he
```

Это и есть проверка, что артефакт настоящий: файл, записанный плейграундом,
читается штатным рантаймом библиотеки без единой правки в нём.

## Что дало исправление аксональных задержек

Контролируемое сравнение: тот же геном, 5 разных seed, по
50000 шагов, «до» — коммит `1cdfe4f582ef`, «после» — `1b80631521d3`.
Метрика — бит/символ на отложенном хвосте, меньше лучше.

| корпус | до исправления | после исправления | разница средних |
|---|---|---|---|
| `shakespeare_en` | 4.837 ± 0.023 | 4.836 ± 0.024 | -0.0010 |
| `synthetic_markov` | 3.447 ± 0.028 | 3.468 ± 0.021 | +0.0206 |

Разница лежит внутри разброса по seed. Это и есть результат: механизм
аксональных задержек 1–5 шагов, вынесенный в заголовок модуля как одна из
ключевых особенностей архитектуры, до исправления фактически не работал —
и его починка **не улучшила предсказание следующего символа**. Задержки на
этой задаче не несут полезного сигнала.

Исправление всё равно нужно: без него код делал не то, что описывает, а любые
дальнейшие выводы о роли задержек были бы выводами о несуществующем механизме.
